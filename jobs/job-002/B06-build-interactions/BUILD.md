# B06-build-interactions

Emit interactions.js containing ONLY mechanisms present in the confirmed interaction spec.

Phase: build. Inputs: confirmed analysis + extracted assets + user data + config.
